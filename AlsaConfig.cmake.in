find_package(PkgConfig REQUIRED)
pkg_search_module(ALSA REQUIRED alsa)
message(STATUS "ALSA include directories: ${ALSA_INCLUDE_DIRS}")
message(STATUS "ALSA library directories: ${ALSA_LIBRARY_DIRS}")
message(STATUS "ALSA libraries: ${ALSA_LIBRARIES}")
message(STATUS "ALSA definitions: ${ALSA_DEFINITIONS}")
message(STATUS "ALSA cflags: ${ALSA_CFLAGS}")
message(STATUS "ALSA ldflags: ${ALSA_LDFLAGS}")
message(STATUS "ALSA ldlibs: ${ALSA_LDLIBS}")
